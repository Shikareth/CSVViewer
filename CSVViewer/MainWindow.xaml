<Window x:Class="CSVViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSVViewer"
        mc:Ignorable="d"
        MinHeight="450" MinWidth="800"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="{Binding Path=WindowTitle, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Initialized="Window_Initialized">
    <Window.Resources>
        <DataTemplate x:Key="DataGrid">
            
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Left">
                <Label Content="Label" Width="200"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
